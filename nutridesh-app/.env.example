# Backend API base URL — change to your dev machine's LAN IP for real device testing
# Phone on same WiFi: use Mac IP e.g. http://192.168.1.50:3000
# Localhost works only for iOS simulator / web
EXPO_PUBLIC_API_URL=http://localhost:3000
